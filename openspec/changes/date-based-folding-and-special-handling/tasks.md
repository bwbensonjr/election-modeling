## 1. Fold schedule by election date

- [x] 1.1 Rewrite `folds.py`: `Fold.year: int` becomes `Fold.key: str` holding the ISO `election_date`; delete `ELIGIBLE_FOLD_YEARS` and `SEED_YEARS_END`; add `SEED_CUTOFF = "2014-01-01"`. Verify by importing `folds` and confirming `build()` on the committed race table returns 25 folds whose keys are the sorted distinct election dates from 2014-01-01 onward.
- [x] 1.2 Rewrite `build()` to slice on `election_date` --- holdout `== key`, train `< key` --- keeping the existing `scoreable` handling on the holdout side only and the `skipped` return for a date a definition empties. Verify that the fold keyed `2016-11-08` has 2016-03-01 and 2016-05-10 in `train` and neither in `holdout`, and that no training race is dated on or after the fold key.
- [x] 1.3 Update `schedule()` to report per fold: the key, the training window as a date range, `n_train`, `n_holdout`, `n_specials`, and whether the date is a general or special-election date. Verify the returned frame has one row per fold plus one per skipped date.
- [x] 1.4 Add a `legmodel folds` CLI subcommand printing the schedule under a named definition, satisfying the spec's "The published schedule states its size". Verify the schedule on the unfiltered race table is 25 folds --- 6 general dates carrying 408 races, 19 special dates carrying 26 races, 199 seed races --- and that under the adopted definition it is 23 folds over 413 races with 2023-05-30 and 2024-03-05 reported as skipped rather than absent, which requires measuring the schedule against the dates in the record rather than only the dates the definition admits.
- [x] 1.5 Update the ten `fold.year` call sites (nine in `score.py`, one in `folds.py`) to `fold.key`, including the `fold` column written into predictions, coefficients and diagnostics, and the `seed_for` argument. Verify `uv run legmodel score --variant baseline --definition current` runs to completion and every output row's `fold` column is a date string.

## 2. Grouping granularity

- [x] 2.1 Add a fold-granularity check to `Variant.validate()`: every `group_effects` column must have each of its levels contained within one `election_date`. Verify a variant declaring `group_effects=("election_year",)` is refused with an error naming the factor and the fold granularity, and one declaring `("election_date",)` validates.
- [x] 2.2 Regroup `baseline_year` and `baseline_year_pres` from `election_year` to `election_date`, keeping `HalfNormal(5)` and `target_accept=0.95` unchanged. Verify both validate and that `uv run legmodel variants` shows the new grouping in their declared formulas.
- [x] 2.3 Confirm the existing per-fold `GroupedPredictorError` path still fires for `baseline_year_pres`: `pres_elec` is constant within every `election_date` level by construction, so it should now be refused on every fold rather than only the early ones. Verify by scoring it on one definition and confirming a refusal row per fold in `fit_diagnostics.csv`. If it is refused everywhere, record that as the outcome rather than working around it --- it is the same evidence the year-grouped contrast arm provided, stated more sharply. **Outcome: refused on all 23 folds**, with `pres_elec` constant within every `election_date` level from the first fold's 199 training races onward. Because a variant with no predictions has no scorecard row, `run()` now catches the all-folds-refused error, appends the 23 refusal rows to `fit_diagnostics.csv`, prints a notice and continues to the next variant, rather than aborting the whole rescore (user decision, option 2).

## 3. Composite variants and the three arms

- [x] 3.1 Add a `not_special` derived boolean column (`~is_special`) to the prepared frame so a component can exclude specials through the existing `requires` mechanism. Verify `variants.restrict()` on a variant requiring `not_special` returns only general elections.
- [x] 3.2 Add `CompositeVariant(name, components, route_on, description)` with `components` mapping a boolean to a `Variant` and `route_on` naming a boolean column. Give it the same `validate()` surface as `Variant`, delegating to each component. Verify a composite naming an absent routing column is refused with the column named.
- [x] 3.3 Branch `score_variant` on a composite: run the existing per-fold loop once per component over that component's restricted races, then concatenate predictions, coefficients and diagnostics. Write `variant` as the composite name and add a `component` column to all three outputs. Verify every holdout race appears exactly once across the concatenated predictions, and that a race matching no component or both raises an error naming the race.
- [x] 3.4 Register the three arms: `special_pooled_term` (baseline predictors plus `is_special`), `special_pooled_plain` (baseline predictors), and `special_split` (composite; general component requires `not_special` and declares no `is_special`, special component trains on all races and declares `is_special`, routed on `is_special`). Verify `uv run legmodel variants` lists all three with the declared formulas of each component.
- [x] 3.5 Retire `baseline_special`, whose predictors are now `special_pooled_term`'s. Verify no registry entry or document references `baseline_special` afterwards.
- [x] 3.6 Assert parity of `special_pooled_plain` against `baseline`: same predictors, so under a shared seed they must produce identical predictions race for race. Note `fit.seed_for` hashes the variant *name*, so the two do **not** share a seed in a normal run and differ by Monte Carlo error alone (max 0.90 margin points over 424 races, against a residual scale near 12.5) --- the parity check therefore forces a common seed. Verify by scoring both under one definition with `seed_for` stubbed to a constant name and diffing prediction, interval bounds, win probability, error and CRPS to exactly zero over all 424 races.

## 4. Segment reporting

- [x] 4.1 Add the three-way ballot-timing segment to `score.py` --- presidential-date general, non-presidential-date general, special --- alongside the existing `pres_elec` and `is_special` breakouts rather than replacing them (design.md, Open Questions). Verify the scorecard carries a row for each of the three with its race count.
- [x] 4.2 Recompute `pres_bias_gap` from general elections only, so the special-election segment cannot move it, and rename the published columns to say `general` where they now mean it. Verify the gap for `baseline` computed the new way differs from the old blended figure and that both are reproducible from `holdout_predictions.csv.gz`.
- [x] 4.3 Update `compare.py`'s small-sample rationale comment at line 139, which names the odd-year folds as the special-election evidence; under a date schedule that is the special-date folds. Verify the comment matches the schedule the code now builds.

## 5. Rescore

- [x] 5.1 Add a `--schedule-change` declaration to `legmodel score` that prints the supersession notice. **Finding: there is no unchanged-variant verification in the codebase to gate.** The `model-scoring` requirement "An untouched variant is verified unchanged" was never implemented --- `_merge_existing` replaces scored cells but verifies nothing --- so this is a pre-existing spec gap, left for its own change (user decision, option 1). What is implemented instead: every published output carries a `fold_schedule` stamp, and an appending run refuses to keep rows carrying a different stamp or none, so figures from two schedules cannot be silently mixed. Verify the notice prints under `--schedule-change`, and that `--append` onto the pre-stamp committed outputs is refused by name.
- [x] 5.2 Smoke-run one variant under one definition end to end, and inspect by hand the fold schedule and the 2016-11-08 training boundary before committing to the full rescore. Verify the training set contains the two 2016 specials and no race dated 2016-11-08 or later.
- [x] 5.3 Run the full rescore across every registered variant and every scored definition under the schedule-change gate. Verify the run completes, and that `fit_diagnostics.csv` is reviewed for folds whose fits failed --- particularly `baseline_year` under its new ~25 sparse levels, most holding 1-3 races.
- [x] 5.4 Run the three-arm comparison and the definition comparison, and commit every regenerated file under `data/models/`. Verify each pairwise arm difference is published with its interval and decided/undecided label, plus the differences restricted to general and to special elections with their counts.
- [x] 5.5 Regenerate `variable_importance.csv` and the threshold sweep under the new schedule. Verify the drop-one arms still score identical races across arms, which the build already fails on if they do not.

## 6. Documents

- [x] 6.1 Correct the three "`pres_elec` is a property of the calendar year" statements in `README.md:473`, `docs/variant_results.md:116` and `docs/variable_importance.md:129`. State instead that it is a property of the election date, that it is near-constant within a calendar year only because no special election has ever fallen on a presidential general date, and that the measured refusals and divergences stand because the harness tests confounding per fold from the data. Verify no document asserts the calendar-year framing afterwards.
- [x] 6.2 Add a fold-schedule section to `docs/scoring.md` describing the date schedule, the seed cutoff, and why a same-year earlier election trains the fold. Verify it states the fold count and the general/special split.
- [x] 6.3 Write up the three-arm result as `docs/special_handling_result.md`, covering the pooled and segmented comparisons and naming the adopted arm, or recording the comparison as undecided if the intervals span zero. Verify every figure quoted is recomputable from the committed outputs.
- [x] 6.4 Update every quoted metric in `README.md`, `docs/variant_results.md`, `docs/variable_importance.md`, `docs/money_results.md`, `docs/definition_result.md` and `docs/is_special_result.md` to the rescored figures, and mark the superseded figures as produced under the previous schedule. Verify no stale number survives by cross-checking each against `scorecard.csv`.
- [x] 6.5 Re-state the `pres_elec` open issue in `README.md:438` under the new schedule: report the drop-one cost as re-measured, and note that arm C changes what the general model's `pres_elec` term is estimated from (design.md, Open Questions). Verify the issue names the current figures rather than the superseded ones.

## 7. Tests for the boundary logic

This group introduces the project's first test framework. It is separable ---
tasks 1-6 verify through the CLI and the published outputs --- but the fold
boundary is where a silent error would poison every number downstream.

- [x] 7.1 Add `pytest` as a dev dependency in `pyproject.toml` and a `tests/` package. Verify `uv run pytest` collects and runs.
- [x] 7.2 Test `folds.build()` on a small synthetic frame: fold count, the strict `<` training boundary, a same-year earlier election landing in training, the seed cutoff excluding earlier races from every holdout, and a date a definition empties being reported as skipped. Verify all pass.
- [x] 7.3 Test the grouping-granularity check and the composite routing: a coarser grouping factor is refused, and a composite routes each race to exactly one component and errors on a race matching none or both. Verify all pass.
